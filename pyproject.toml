[project]
name = "apa7-bib-validator"
version = "0.1.0"
description = "A command-line tool to validate APA-7 formatted bibliography entries in a Word (DOCX) document."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-docx>=1.2.0",
    "rich>=14.0.0",
]
